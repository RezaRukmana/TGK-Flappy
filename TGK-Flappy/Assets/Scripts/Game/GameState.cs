using UnityEngine;
using System.Collections;

public class GameState : MonoBehaviour {

	public static int gameState; //0 = playing, 1 = game over

	// Use this for initialization
	void Start () {
		gameState = 0;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
